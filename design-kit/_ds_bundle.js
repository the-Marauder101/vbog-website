/* @ds-bundle: {"kind":"tokens-only","globalName":"VBOG"} */
/* VBOG is a tokens-and-CSS design kit: there is no compiled React component
   library in this repo, so the bundle is intentionally empty-bodied.
   Build with the classes and custom properties defined in styles.css. */
(function () {
  window.VBOG = window.VBOG || {};
})();
